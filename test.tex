\documentclass{article}
\usepackage{synttree}
\begin{document}
A sentence.
\childattachsep{0.6in} 
\childsidesep{0.9em}
Another one.

    \synttree[A[ A    ]] % single child, no label issues

    \synttree[ A [ AA ]] % single child, bottom label is wider

    \synttree[AA[A]] % single child, top label is wider

    \synttree[A[.t AA]] % single child, triangle

    \synttree[A[ b a a    a a a b][D]]

    \synttree[A[aa a a a a a aa [E][.t FFFFFFFF]][D[.t ZZ]]]
    % with some triangles, slightly weird.

    \synttree[A[B][C][D]] % three children

    \synttree{4}[A[B[E[F]][C]][D[.b Z]]]
\end{document}
